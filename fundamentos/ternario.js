/* o simbolo "=>" indica a função Arrow, que significa uma sunção com poucos parâmetros e por isos não precisa de "{}" */
const resultado = nota => nota >= 7 ? 'Aprovado':'Reprovado'

console.log(resultado(7.1))
console.log(resultado(6.7))

